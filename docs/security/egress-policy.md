# Egress Policy

## Purpose
Default egress denies network and webhooks, asks for external URLs, and allows clipboard/file export only after redaction.

## Related Commands
See [security commands and implemented code](index.md).
